streamlit>=1.42.0
groq>=0.18.0
python-dotenv>=1.0.1
pydantic>=2.10.6
pandas>=2.2.3
loguru>=0.7.3
tiktoken>=0.8.0
tenacity>=9.0.0
pytest>=8.3.4
pytest-asyncio>=0.23.0
portalocker>=3.2.0
emoji
nest-asyncio>=1.6.0
pymongo>=4.6
hindsight-client
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# NOTE: cascadeflow[all] was removed — its `[all]` extra pulls in vllm and other
# heavy ML packages that fail to build on Windows. The app falls back to built-in
# manual routing when cascadeflow is absent. If you want the official CascadeFlow
# SDK, try `pip install cascadeflow` (WITHOUT [all]) separately; it's optional.
